export enum TURN {
    GAME_START,
    FIRST_TURN,
    SECOND_TURN,
    PENULTIMATE_TURN = 15,
    LAST_TURN
}

export enum CARD_TYPE {
    CROP = 'CROP',
    GOOD = 'GOOD'
}

export enum CROP {
    CORN = 'CORN'
}